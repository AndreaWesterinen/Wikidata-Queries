SELECT ?item ?nl
  (COUNT(DISTINCT ?p) as ?count)
  (SAMPLE(?p) as ?sample_p)
{
    ?item wdt:P407 wd:Q9129 ;
          wdt:P31 wd:Q12308941 ;
          wdt:P1705 ?nl . 
    FILTER( regex( str(?nl), "[^οαη][^ς]$") )
    OPTIONAL { ?p wdt:P735 ?item }
}
GROUP BY ?item ?nl
ORDER BY DESC(?count) ?nl
