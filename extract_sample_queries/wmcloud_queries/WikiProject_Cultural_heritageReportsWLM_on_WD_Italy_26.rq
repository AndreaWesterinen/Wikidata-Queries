SELECT DISTINCT ?item ?itemLabel ?start ?end WHERE {
  ?item wdt:P131* wd:Q38. 
  ?item p:P2186 ?wlmst .
  ?wlmst pq:P580 ?start; pq:P582 ?end .
  FILTER(?start > ?end)
  SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE],it". }
}
