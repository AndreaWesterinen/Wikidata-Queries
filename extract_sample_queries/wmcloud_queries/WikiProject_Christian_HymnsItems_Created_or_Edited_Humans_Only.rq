SELECT DISTINCT ?item ?itemLabel WHERE {
  ?item wdt:P5008 wd:Q100975219.
  ?item wdt:P31 wd:Q5
  FILTER(NOT EXISTS { ?item wdt:P1855 ?o. })
  SERVICE wikibase:label { bd:serviceParam wikibase:language "en". }
}
ORDER BY (LCASE(?itemLabel))
