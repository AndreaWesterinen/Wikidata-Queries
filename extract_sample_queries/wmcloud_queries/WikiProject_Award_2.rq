SELECT ?item (SAMPLE(COALESCE(?en_label, ?item_label)) as ?label)
WHERE
{
	?item wdt:P31 wd:Q26877490 .
  OPTIONAL {?item rdfs:label ?en_label . FILTER(LANG(?en_label) = "en")}
  OPTIONAL {?item rdfs:label ?item_label}
} group by ?item
