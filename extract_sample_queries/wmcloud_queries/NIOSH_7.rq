#superseded in title
SELECT ?item ?itemLabel ?title ?repby
WHERE 
{
  ?item wdt:P5008 wd:Q104416361.
  ?item wdt:P1476 ?title.
  OPTIONAL {?item wdt:P1366 ?repby.}
  FILTER CONTAINS(?title, "superseded") 
  
  SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE],en". }
}
