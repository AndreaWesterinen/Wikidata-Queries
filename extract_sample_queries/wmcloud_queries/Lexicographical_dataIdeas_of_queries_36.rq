SELECT ?l ?lemma ?sense WHERE {
   ?l a ontolex:LexicalEntry ; dct:language ?language ;
        wikibase:lemma ?lemma ;
        ontolex:sense/skos:definition ?sense.
  ?language wdt:P218 'en'.
  filter(regex(?sense, ".*heraldic.*") || regex(?sense, ".*heraldry.*"))
}
