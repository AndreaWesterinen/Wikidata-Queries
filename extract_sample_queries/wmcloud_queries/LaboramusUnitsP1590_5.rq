SELECT ?unit (count(?x) as ?count) WHERE {
     { ?x p:P1590/psv:P1590 [ wikibase:quantityUnit ?unit ] }
     UNION
     { ?x pqv:P1590 [ wikibase:quantityUnit ?unit ] }
} GROUP BY ?unit
ORDER BY DESC(?count)
