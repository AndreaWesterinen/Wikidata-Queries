#title: Items with statements where property P11536 uses the novalue special value
SELECT ?item ?itemLabel (GROUP_CONCAT(?instanceLabel; separator=", ") as ?instanceLabels)
{
  ?item p:P11536 [a wdno:P11536; a wikibase:BestRank] .

  OPTIONAL {?item wdt:P31 ?instance}
  SERVICE wikibase:label {
    bd:serviceParam wikibase:language "en,en,ru,ja,zh" .
    ?item rdfs:label ?itemLabel .
    ?instance rdfs:label ?instanceLabel
  }
}
GROUP BY ?item ?itemLabel
LIMIT 100
