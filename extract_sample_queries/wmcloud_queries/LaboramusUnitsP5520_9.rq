SELECT ?unit (count(?x) as ?count) WHERE {
     { ?x p:P5520/psv:P5520 [ wikibase:quantityUnit ?unit ] }
     UNION
     { ?x pqv:P5520 [ wikibase:quantityUnit ?unit ] }
} GROUP BY ?unit
ORDER BY DESC(?count)
