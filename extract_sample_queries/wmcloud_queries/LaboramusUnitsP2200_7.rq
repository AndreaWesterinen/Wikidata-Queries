SELECT ?unit (count(?x) as ?count) WHERE {
     { ?x p:P2200/psv:P2200 [ wikibase:quantityUnit ?unit ] }
     UNION
     { ?x pqv:P2200 [ wikibase:quantityUnit ?unit ] }
} GROUP BY ?unit
ORDER BY DESC(?count)
