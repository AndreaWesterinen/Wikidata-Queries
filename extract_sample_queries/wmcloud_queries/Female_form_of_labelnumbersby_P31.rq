SELECT ?item (COUNT(DISTINCT(?withlabel)) as ?ct) { ?withlabel wdt:P2521 ?femalelabel . OPTIONAL { ?withlabel wdt:P31 ?hasinstance . } BIND(IF(BOUND(?hasinstance), ?hasinstance, wd:Q22260099) as ?item) . } GROUP BY ?item ORDER BY DESC(?ct) ?item
