#Lexemes with a sense but no P5137
select distinct (count(?lexemeId) as ?count) #?lemma 
WHERE {
  ?lexemeId a ontolex:LexicalEntry ;
            #dct:language wd:Q9035;
            #wikibase:lemma ?lemma;
            ontolex:sense ?sense.
  filter not exists {?sense wdt:P5137 []}.
}
