SELECT ?grouping (COUNT(DISTINCT ?entity) as ?count) (SAMPLE(?entity) as ?sample) WHERE { ?entity wdt:P31 wd:Q193512. ?entity wdt:P31 ?grouping .} GROUP BY ?grouping HAVING (?count < 12) ORDER BY DESC(?count) LIMIT 1000
