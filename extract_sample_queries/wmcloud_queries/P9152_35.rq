SELECT ?item ?itemLabel ?value
WHERE
{
	{
		SELECT DISTINCT ?item ?property ?value {
			?item wdt:P9152 ?value .
          MINUS { ?item rdfs:label ?label .
                 FILTER( LANG(?label)="nl" ) }			
		} LIMIT 100
	} .
	SERVICE wikibase:label { bd:serviceParam wikibase:language "en" } .
}
