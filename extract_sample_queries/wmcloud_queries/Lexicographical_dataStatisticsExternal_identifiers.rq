select ?item (count(*) as ?count) {
  ?item wikibase:propertyType wikibase:ExternalId;
        wikibase:directClaim ?wdt;
        wdt:P31/wdt:P279* wd:Q54076056.
  [] ?wdt [].
} group by ?item order by desc(?count) ?item
