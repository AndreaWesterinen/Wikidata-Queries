SELECT (wd:Q2481549 as ?item) ("people born in present-day Slovakia" as ?legend) (GROUP_CONCAT(?p;separator = ",") as ?labels) (GROUP_CONCAT(?ct;separator = ",") as ?values)
{
	{
		SELECT  ?p (COUNT(DISTINCT ?mainsubject) as ?ct)
		{
			?entry 	wdt:P31 wd:Q19389637
  			; 		wdt:P1433 wd:Q665807 .
          	?entry wdt:P921 ?mainsubject .
          	?mainsubject wdt:P31 wd:Q5 ; wdt:P19 ?pob . ?pob wdt:P17 wd:Q214 .
          	OPTIONAL { ?dob schema:about ?mainsubject ; schema:isPartOf <https://hu.wikipedia.org/>  }
          	OPTIONAL { ?dod schema:about ?mainsubject ; schema:isPartOf <https://de.wikipedia.org/>  }
		BIND(CONCAT(IF(!BOUND(?dob),"---","huwiki-"),IF(!BOUND(?dod),"---","-dewiki")) as ?p)
		}
		GROUP BY  ?p
		ORDER BY ASC(?p)                  
	}
}
