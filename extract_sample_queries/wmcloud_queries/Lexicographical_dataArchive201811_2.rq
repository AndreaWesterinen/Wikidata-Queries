SELECT ?l ?lemma WHERE {
   ?l a ontolex:LexicalEntry ; dct:language ?language ;
        wikibase:lemma ?lemma .
  ?language wdt:P218 'eo'
  FILTER NOT EXISTS {?l ontolex:sense ?sense }
} ORDER BY ?lemma
