SELECT ?item (COUNT(DISTINCT ?st) as ?count)
WHERE
{
    ?item wdt:P407 wd:Q652 ;
          wdt:P31/wdt:P279? wd:Q202444 . 
    ?item p:P282 ?st
}
GROUP BY ?item
HAVING (?count > 0)
ORDER BY DESC(?count)
LIMIT 10
