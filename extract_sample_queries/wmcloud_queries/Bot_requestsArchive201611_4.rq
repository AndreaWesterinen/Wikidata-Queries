SELECT DISTINCT ?article ?articleLabel ?category ?categoryLabel ?commonsCategory
{
    # ?article and item2 share the same P373 ("Commons Category")
	?article  wdt:P373 ?commonsCategory .
	?category wdt:P373 ?commonsCategory .
  	FILTER(?article != ?category) .   

    # ?article is an "article" item and and item2 is an "category" item
	?category wdt:P31 wd:Q4167836 .
  	MINUS {?article wdt:P31 wd:Q4167836 } .
  
    # P301 and P910 are not set
	MINUS {?article  wdt:P301 [] } .
	MINUS {?category wdt:P301 [] } .
	MINUS {?article  wdt:P910 [] } .
	MINUS {?category wdt:P910 [] } .

	SERVICE wikibase:label { bd:serviceParam wikibase:language "en,en" } .
} LIMIT 500
