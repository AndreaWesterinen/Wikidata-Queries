PREFIX schema: <http://schema.org/>

#Missing description for a target language
#Search for missing descriptions in a target language.
#Can be input for Q29032512
SELECT ?item ?itemDescription ?Staat ?StaatLabel WHERE {
  ?item wdt:P31 wd:Q12280.
  OPTIONAL {  }
  OPTIONAL {  }
  OPTIONAL {  }
  SERVICE wikibase:label { bd:serviceParam wikibase:language "de". }
  
  FILTER(NOT EXISTS {
    ?item schema:description ?itemdesc.
    FILTER((LANG(?itemdesc)) = "de")
  })
  OPTIONAL { ?item wdt:P17 ?Staat. }
}
ORDER BY ?itemLabel
