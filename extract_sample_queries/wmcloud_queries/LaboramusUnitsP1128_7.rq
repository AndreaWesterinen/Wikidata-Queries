SELECT ?unit (count(?x) as ?count) WHERE {
     { ?x p:P1128/psv:P1128 [ wikibase:quantityUnit ?unit ] }
     UNION
     { ?x pqv:P1128 [ wikibase:quantityUnit ?unit ] }
} GROUP BY ?unit
ORDER BY DESC(?count)
