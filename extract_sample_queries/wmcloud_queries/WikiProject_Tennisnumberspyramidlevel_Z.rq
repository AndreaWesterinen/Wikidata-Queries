SELECT 
   (wd:Q847 as ?item) 
   (SAMPLE(?a) as ?sample) 
   (COUNT(DISTINCT ?a) as ?ct)
   (wd:Q847 as ?type)
("{{{level}}}" as ?level)
("{{{descr}}}" as ?descr)
WHERE
   { ?a wdt:P641 wd:Q847 ; wikibase:statements 1  }
