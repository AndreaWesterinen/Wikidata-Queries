SELECT ?item (COUNT(?item) as ?count) WHERE {
  ?painting wdt:P31 wd:Q3305213 ;
            wdt:P195 ?item ;
            wdt:P4765 ?image .
} GROUP BY ?item
HAVING (?count > 5)
ORDER BY DESC(?count) 
LIMIT 250
