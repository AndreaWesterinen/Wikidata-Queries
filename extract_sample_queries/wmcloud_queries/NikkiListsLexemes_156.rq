select ?item (replace(str(?item),str(wd:),"") as ?qid) (count(?item) as ?total) ("P443" as ?p) {
  ?st pq:P407 ?item.
  ?item2 p:P443 ?st.
  minus { ?item2 ontolex:representation [] }
} group by ?item ?qid order by desc(?total) ?item ?p
