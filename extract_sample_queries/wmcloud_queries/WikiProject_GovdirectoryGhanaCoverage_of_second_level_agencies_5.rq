SELECT ?grouping (COUNT(DISTINCT ?entity) as ?count) (SAMPLE(?entity) as ?sample) WHERE { ?entity wdt:P31 wd:Q545769.
  MINUS{ ?entity wdt:P576 [] }
  MINUS{ ?entity wdt:P1366 [] }. ?entity wdt:P31 ?grouping .} GROUP BY ?grouping HAVING (?count < 1) ORDER BY DESC(?count) LIMIT 1000
