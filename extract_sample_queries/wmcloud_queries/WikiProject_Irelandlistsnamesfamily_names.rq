SELECT ?item ?itemLabel (COUNT(DISTINCT ?p) as ?count) (sample(?p) as ?sample)
{
    ?p wdt:P27 wd:Q27 ; wdt:P31 wd:Q5 ; wdt:P734 ?item  .
    SERVICE wikibase:label { bd:serviceParam wikibase:language "en" }
}
GROUP BY ?item ?itemLabel
ORDER BY DESC(?count) ?itemLabel
LIMIT 200
