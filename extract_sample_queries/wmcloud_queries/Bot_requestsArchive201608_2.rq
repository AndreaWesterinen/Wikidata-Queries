SELECT (count(?n_articles) AS ?n_items) ?n_articles WHERE{
  {
    SELECT (count(?item) as ?n_articles) WHERE{
      ?item wdt:P1343/^schema:about/schema:isPartOf <https://ru.wikisource.org/>
    } GROUP BY ?item
  }
} GROUP BY ?n_articles
