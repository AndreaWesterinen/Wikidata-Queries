SELECT ?item (COUNT(DISTINCT ?st) as ?count)
WHERE
{
    ?item wdt:P407 wd:Q9129 ;
          wdt:P31/wdt:P279? wd:Q202444 . 
    ?item p:P1705 ?st
}
GROUP BY ?item
HAVING (?count > 1)
ORDER BY DESC(?count)
