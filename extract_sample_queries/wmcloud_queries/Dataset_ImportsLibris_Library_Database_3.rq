# Libraries in Sweden on a map
#defaultView:Map
SELECT DISTINCT ?item ?itemLabel ?coords WHERE {
  ?item (wdt:P31/wdt:P279*) wd:Q7075.
  ?item wdt:P17 wd:Q34.
  ?item wdt:P625 ?coords.
  SERVICE wikibase:label { bd:serviceParam wikibase:language "sv". }
}
