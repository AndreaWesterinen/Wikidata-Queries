SELECT ?item ?itemLabel ?kod WHERE {
  ?item wdt:P17 wd:Q159.
  FILTER(!(ISBLANK(?kod)))
  SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE],en". }
  OPTIONAL { ?item wdt:P395 ?kod. }
  FILTER NOT EXISTS {?item wdt:P31/wdt:P279* wd:Q43263}
}
ORDER BY ASC(?kod)
