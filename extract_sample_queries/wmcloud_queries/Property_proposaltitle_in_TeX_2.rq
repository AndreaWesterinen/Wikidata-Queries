#Labels containing HTML escape sequences
#added before 2016-10

SELECT DISTINCT ?item ?label
WHERE
{
  ?item wdt:P31 wd:Q13442814;
        rdfs:label ?label.
  FILTER CONTAINS(?label, "&amp").
}
LIMIT 10
