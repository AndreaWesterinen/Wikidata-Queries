SELECT ?item ?country ?date (year(xsd:dateTime(?date)) as ?year)
WHERE 
{
  ?item wdt:P31/wdt:P279* wd:Q40231 .
  ?item wdt:P585 ?date .
  OPTIONAL { ?item wdt:P17 ?country . }
  FILTER(?date > NOW())
  SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE],en". }
}
