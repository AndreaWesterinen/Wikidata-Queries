SELECT distinct ?item ?itemLabel ?temp ?tempLabel WHERE {
  ?item wdt:P31/wdt:P279* wd:Q7257461. 
  Optional { ?item wdt:P1424 ?temp. }
  SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE],en". }
} order by ?itemLabel
