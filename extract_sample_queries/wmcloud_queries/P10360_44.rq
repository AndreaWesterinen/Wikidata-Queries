#title:Count of items with P10360 by number of statements
#defaultView:AreaChart
SELECT ?st ?ct {
  {
    BIND (0 AS ?ct)
    BIND (0 AS ?st)
  }
  UNION {
    SELECT ?st (COUNT(*) as ?ct) {
      ?item wdt:P10360 ?value ; wikibase:statements ?st
    }
    GROUP BY ?st ORDER BY ?st
  }
}
