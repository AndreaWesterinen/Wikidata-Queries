##defaultView:BubbleChart
#Most cited authors who have published on Zika and do not have an ORCID statement
SELECT ?author ?authorLabel (COUNT(?publication) AS ?count)
WHERE
{
    ?item wdt:P2860 ?publication .
    { ?publication wdt:P921 wd:Q202864 . } UNION { ?publication wdt:P921 wd:Q8071861 . }
    ?publication wdt:P50 ?author .
    MINUS { ?author wdt:P496 ?orcid . }
    MINUS {
      ?author wdt:P570 ?dod
      FILTER (YEAR(?dod) < 2012)
    }
    SERVICE wikibase:label {
        bd:serviceParam wikibase:language "en" .
    }
}
GROUP BY ?author ?authorLabel
ORDER BY DESC(?count)
