# Query to find the most connected-to items in class ?c = 
  # pointed to by property ?prop = 
  # Query generated by [[Template:MostConnectedToInstances]]

SELECT ?b ?bLabel ?count WHERE {
  {
    SELECT ?b (COUNT(*) AS ?count) WHERE {
       ?a ?prop ?b .
       ?b wdt:P31 ?c .
       BIND (wd: AS ?c) .
       BIND (wdt: AS ?prop)
    } GROUP BY ?b
  } . 
  SERVICE wikibase:label {
    bd:serviceParam wikibase:language "en" .
  }
}
# HAVING (?count > 1)
ORDER BY DESC(?count) ?bLabel
LIMIT 100
