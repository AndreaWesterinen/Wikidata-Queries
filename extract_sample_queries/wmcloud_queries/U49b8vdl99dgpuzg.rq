SELECT ?item ?value WHERE {
  ?ref pr:P4656 ?value .
  ?statement prov:wasDerivedFrom ?ref.
  ?item ?p ?statement .
  BIND( REGEX( STR( ?value ), "alexa|whois" ) AS ?regexresult ) .
  FILTER( ?regexresult = true ) .
} 
