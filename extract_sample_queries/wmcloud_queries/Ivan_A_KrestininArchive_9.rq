SELECT ?item ?itemLabel ?value

WHERE
{
  ?item wdt:P31 ?value .

  FILTER (?value = wd:Q4022)
  
  FILTER NOT EXISTS { ?item wdt:P17 ?x }
  
  FILTER EXISTS { ?wfr schema:about ?item . ?wfr schema:inLanguage "en" }
  
  SERVICE wikibase:label { bd:serviceParam wikibase:language "en"  }    
}
