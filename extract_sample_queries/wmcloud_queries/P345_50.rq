SELECT ?item ?value WHERE
{
  ?item p:P345/ps:P345 ?value .
  
  FILTER( REGEX(REPLACE(STR(?value), "%20", " "), "^((https:\\/\\/(m\\.|www\\.|)imdb\\.com)?\\/?(title\\/(tt)|name\\/(nm))(\\d{7,8})(\\/?.*)?)$") )
}
