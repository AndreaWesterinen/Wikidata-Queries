SELECT 
   (wd:Q847 as ?item) 
   (SAMPLE(?a) as ?sample) 
   (COUNT(DISTINCT ?a) as ?ct)
   (wd:Q47443726 as ?type)
("{{{level}}}" as ?level)
("{{{descr}}}" as ?descr)
WHERE
   { ?a wdt:P279 wd:Q13219666 }
