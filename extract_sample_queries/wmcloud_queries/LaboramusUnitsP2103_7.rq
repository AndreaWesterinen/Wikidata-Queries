SELECT ?unit (count(?x) as ?count) WHERE {
     { ?x p:P2103/psv:P2103 [ wikibase:quantityUnit ?unit ] }
     UNION
     { ?x pqv:P2103 [ wikibase:quantityUnit ?unit ] }
} GROUP BY ?unit
ORDER BY DESC(?count)
