SELECT DISTINCT ?item where {
 ?item wdt:P31 wd:Q5 ; wikibase:sitelinks ?slct . 
 FILTER ( ?slct = 0 ) .
 OPTIONAL { ?item wdt:P21 ?gender } .
 FILTER ( !BOUND (?gender ) ) .
 BIND (xsd:integer(STRAFTER(str(?item), "Q")) AS ?qid)
}  ORDER BY DESC(?qid) LIMIT 1000
