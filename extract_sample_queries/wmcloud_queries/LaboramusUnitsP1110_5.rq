SELECT ?unit (count(?x) as ?count) WHERE {
     { ?x p:P1110/psv:P1110 [ wikibase:quantityUnit ?unit ] }
     UNION
     { ?x pqv:P1110 [ wikibase:quantityUnit ?unit ] }
} GROUP BY ?unit
ORDER BY DESC(?count)
