SELECT ?item WHERE {
  ?item wdt:P31 wd:Q5 .
  FILTER( xsd:integer( STRAFTER( STR( ?item ), STR( wd:Q ) ) ) > 10000000 ) .
  ?item rdfs:label ?label FILTER( LANG( ?label ) = 'en' ) .
}
LIMIT 1000
#do not delete
