#title: values of property P2098 with most sitelinks
SELECT ?item ?itemLabel ?itemDescription ?sls
WHERE
{
	?p wdt:P2098 ?item . 
    ?item wikibase:sitelinks ?sls 
    SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE],en". }      
}
ORDER BY DESC(?sls) ?item
LIMIT 100
