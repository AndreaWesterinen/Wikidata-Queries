SELECT 
  (wd:Q5 as ?item)
  ("P22" as ?in)
  ("P25" as ?out)
  (COUNT(DISTINCT ?item) as ?all)
  (COUNT(DISTINCT ?has) as ?with)
{
    ?item wdt:P22 [] ; wdt:P31 wd:Q5 .
    OPTIONAL { ?item wdt:P25 ?relative . BIND(?relative as ?has)  }
}
