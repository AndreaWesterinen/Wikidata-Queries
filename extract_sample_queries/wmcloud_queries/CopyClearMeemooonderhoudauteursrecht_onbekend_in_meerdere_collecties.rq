SELECT ?item (COUNT(?collectie) AS ?collectieCount) {
  ?item wdt:P6379 ?collectie.
  ?item wdt:P6379 ?collectie2.
  ?item wdt:P31 wd:Q5.
  ?collectie wdt:P17 wd:Q31.
  FILTER NOT EXISTS {
    ?item wdt:P7763 [].
  }
}
GROUP BY ?item
HAVING (?collectieCount > 1)
ORDER BY DESC (?collectieCount)
