SELECT ?unit (count(?x) as ?count) WHERE {
     { ?x p:P1107/psv:P1107 [ wikibase:quantityUnit ?unit ] }
     UNION
     { ?x pqv:P1107 [ wikibase:quantityUnit ?unit ] }
} GROUP BY ?unit
ORDER BY DESC(?count)
