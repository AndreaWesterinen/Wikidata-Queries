SELECT ?item ?label WHERE {
  ?item wdt:P106 wd:Q901.
  SERVICE wikibase:label {
    bd:serviceParam wikibase:language "en, de, fr, pt, es".
    ?item rdfs:label ?label.
  }
  FILTER(NOT EXISTS {
    ?item rdfs:label ?lang_label.
    FILTER((LANG(?lang_label)) = "sq")
  })
}
