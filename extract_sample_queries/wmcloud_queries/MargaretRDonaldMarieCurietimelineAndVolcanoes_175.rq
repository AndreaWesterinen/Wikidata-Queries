# Fungi taxa without taxon authors 
SELECT ?item ?taxon_name   WHERE {
  SERVICE wikibase:label { bd:serviceParam wikibase:language "en". }
  ?item wdt:P171* wd:Q764.
  ?item p:P225 ?stat . 
  ?stat ps:P225 ?taxon_name.
  MINUS {?stat pq:P405 ?taxon_author.} 
}
