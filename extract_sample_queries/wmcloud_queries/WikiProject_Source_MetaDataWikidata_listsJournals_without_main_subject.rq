SELECT DISTINCT ?item ?linkcount WHERE {
  { ?item wdt:P236 ?issn .} UNION {?item wdt:P1055 ?nlmID .}
  ?item wikibase:sitelinks ?linkcount .
  MINUS { ?item wdt:P921 ?topic }
}
ORDER BY DESC(?linkcount)
LIMIT 3000
