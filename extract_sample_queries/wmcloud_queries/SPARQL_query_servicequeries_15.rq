SELECT ?instance ?instanceLabel (COUNT(?film) AS ?count) WHERE {
  ?film wdt:P345 ?imdb FILTER (SUBSTR(?imdb,1,3)='tt4') .
  ?film wdt:P31 ?instance
  SERVICE wikibase:label {bd:serviceParam wikibase:language "en"}
}
GROUP BY ?instance ?instanceLabel
ORDER BY DESC(?count)
