SELECT ?item ?value WHERE
{
  ?item p:P973/ps:P973 ?value .
  
  FILTER( REGEX(REPLACE(STR(?value), "%20", " "), "^(https?://www\\.tcm\\.turner\\.com/title/title\\.jsp?stid=([1-9][0-9]*)([^\\d]*))$") )
}
