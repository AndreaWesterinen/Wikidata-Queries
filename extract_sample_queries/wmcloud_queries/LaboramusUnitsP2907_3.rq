SELECT ?unit (count(?x) as ?count) WHERE {
     { ?x p:P2907/psv:P2907 [ wikibase:quantityUnit ?unit ] }
     UNION
     { ?x pqv:P2907 [ wikibase:quantityUnit ?unit ] }
} GROUP BY ?unit
ORDER BY DESC(?count)
