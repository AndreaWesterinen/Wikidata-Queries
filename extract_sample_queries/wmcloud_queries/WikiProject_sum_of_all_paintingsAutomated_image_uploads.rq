SELECT ?item ?downloadurl WHERE {
  ?item p:P4765/ps:P4765 ?downloadurl ;
        wdt:P170/wdt:P570 ?dod . 
  BIND(YEAR(?dod) AS ?deathyear)
  FILTER(?deathyear < (YEAR(NOW())-95)) .
  } 
  LIMIT 15000
