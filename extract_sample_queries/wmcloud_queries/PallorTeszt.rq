SELECT ?grouping (COUNT(DISTINCT ?entity) as ?count) (SAMPLE(?entity) as ?sample) WHERE { ?entity wdt:P31 wd:Q5. ?entity wdt:P27 ?grouping .} GROUP BY ?grouping HAVING (?count >= 10000) ORDER BY DESC(?count) LIMIT 1000
