SELECT ?item ?itemLabel ?startdatum WHERE {
  SERVICE wikibase:label { bd:serviceParam wikibase:language "da,[AUTO_LANGUAGE],en". }
  ?item wdt:P179 wd:Q12304882.
  OPTIONAL { ?item wdt:P580 ?startdatum. }
}
ORDER BY (?startdatum)
