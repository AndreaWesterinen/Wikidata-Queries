sparql?query=#defaultView:Map
SELECT ?item ?itemLabel ?geo ?pic WHERE {
  {
    ?item (wdt:P31/(wdt:P279*)) wd:Q41176;
      wdt:P149 wd:Q34636;
      wdt:P625 ?geo.
    OPTIONAL { ?item wdt:P18 ?pic. }
  }
  { SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE],en". } }
}
