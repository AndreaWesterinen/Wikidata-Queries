SELECT ?item ?value WHERE
{
  ?item p:P973/ps:P973 ?value .
  
  FILTER( REGEX(REPLACE(STR(?value), "%20", " "), "^(https?://(www\\.)?metmuseum\\.org/(art/collection/search|collections/search-the-collections|collection/the-collection-online/search)/(\\d+)/?)$") )
}
