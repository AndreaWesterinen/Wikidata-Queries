SELECT ?subject ?subjectLabel ?instance ?instanceLabel (year(?date) as ?year) ?date  WHERE {
   ?subject p:P1249/psv:P1249 ?date_node .
   ?date_node wikibase:timeValue ?date .
   FILTER ((?date) >= (now())) .
   ?subject wdt:P31 ?instance .
  SERVICE wikibase:label {
    bd:serviceParam wikibase:language "en,en" .
  }
}
ORDER BY DESC(?date)
LIMIT 100
