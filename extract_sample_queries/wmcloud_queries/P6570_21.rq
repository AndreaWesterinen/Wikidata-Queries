#title: Property should be used on items with a sitelink to Wikisource
# Complex constraint from [[Property talk:P6570]]
#
SELECT ?item 
WHERE
{
	?item wdt:P6570 [] .
    FILTER NOT EXISTS { ?item ^ schema:about / schema:isPartOf / wikibase:wikiGroup "wikisource" } 
}
LIMIT 100
