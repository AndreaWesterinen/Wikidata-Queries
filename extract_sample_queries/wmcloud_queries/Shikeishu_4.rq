# Manga without author claim
SELECT ?item ?itemLabel WHERE {
    ?item wdt:P31/wdt:P279* wd:Q8274 .
    FILTER NOT EXISTS { ?item wdt:P50 ?language }
  	SERVICE wikibase:label {
		bd:serviceParam wikibase:language "en" .
	}
}
