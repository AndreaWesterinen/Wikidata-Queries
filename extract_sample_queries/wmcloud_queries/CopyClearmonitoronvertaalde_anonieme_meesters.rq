SELECT DISTINCT ?item WHERE {
  ?item wdt:P6379 [].
  ?item wdt:P31 wd:Q5.
  ?item rdfs:label ?label.
  ?item wdt:P2868 wd:Q474968.
  #SERVICE wikibase:label { bd:serviceParam wikibase:language "nl, en". }
  FILTER((LANG(?label)) = "nl")
  FILTER(REGEX(?label, "(Master
