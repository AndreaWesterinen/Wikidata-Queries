SELECT ?item ?value WHERE
{
  ?item p:P973/ps:P973 ?value .
  
  FILTER( REGEX(REPLACE(STR(?value), "%20", " "), "^(https?://(www\\.)?rottentomatoes\\.com/(m|celebrity|tv|critic|franchise)(/[^/]+)/?)$") )
}
