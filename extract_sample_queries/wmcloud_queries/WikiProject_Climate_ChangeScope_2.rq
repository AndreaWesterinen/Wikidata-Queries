SELECT ?grouping (COUNT(DISTINCT ?entity) as ?count) (SAMPLE(?entity) as ?sample) WHERE { ?entity wdt:P5008 wd:Q69524601. ?entity wdt:P31 ?grouping .} GROUP BY ?grouping HAVING (?count >= 5) ORDER BY DESC(?count) LIMIT 1000
