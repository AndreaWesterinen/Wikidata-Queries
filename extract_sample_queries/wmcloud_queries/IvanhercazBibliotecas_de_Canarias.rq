SELECT DISTINCT ?item ?itemLabel ?coordenadas WHERE {
  ?item (wdt:P31/(wdt:P279*)) wd:Q7075;
    wdt:P17 wd:Q29;
    wdt:P131 ?municipio.
  
  ?municipio wdt:P131* wd:Q5813
  OPTIONAL { ?item wdt:P625 ?coordenadas. }
}
