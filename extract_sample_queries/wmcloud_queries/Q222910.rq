SELECT ?item ?itemLabel ?itemDescription ?sitelinks WHERE { 
  ?item wdt:P31 wd:Q105420 .
  ?item wikibase:sitelinks ?sitelinks
  SERVICE wikibase:label { bd:serviceParam wikibase:language "en". } }
ORDER BY DESC(?sitelinks)
#LIMIT 500
