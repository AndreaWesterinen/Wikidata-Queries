SELECT ?item ?itemLabel ?coor ?hascoor WHERE {
    ?item wdt:P170 wd:Q29963974 .
     OPTIONAL { ?item wdt:P625 ?coor . BIND("🌐" as ?hascoor) }
	SERVICE wikibase:label {
		bd:serviceParam wikibase:language "cs" .
        }
}
