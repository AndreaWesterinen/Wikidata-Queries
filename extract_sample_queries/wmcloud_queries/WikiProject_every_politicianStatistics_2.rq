SELECT ?grouping (COUNT(DISTINCT ?entity) as ?count) (SAMPLE(?entity) as ?sample) WHERE { ?entity wdt:P106 wd:Q82955. ?entity wdt:P27 ?grouping .} GROUP BY ?grouping HAVING (?count >= 200) ORDER BY DESC(?count) LIMIT 1000
