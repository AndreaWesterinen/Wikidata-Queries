#Items with Museum Universe Data File ID
#defaultView:Map
SELECT ?item ?itemLabel ?coords WHERE {
  ?item wdt:P6006 ?museum_id.
  OPTIONAL { ?item wdt:P625 ?coords. }
  SERVICE wikibase:label { bd:serviceParam wikibase:language "[AUTO_LANGUAGE],en". }
}
