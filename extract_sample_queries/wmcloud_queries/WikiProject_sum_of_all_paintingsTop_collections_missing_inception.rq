SELECT ?item (COUNT(?item) as ?count) WHERE {
  ?painting wdt:P31 wd:Q3305213 .  
  MINUS { ?painting wdt:P571 [] }
  ?painting wdt:P195 ?item . 
} GROUP BY ?item ?name
HAVING (?count > 10)
ORDER BY DESC(?count) 
LIMIT 250
