SELECT DISTINCT ?item ?itemLabel WHERE {
  SERVICE wikibase:label { bd:serviceParam wikibase:language "nl, en". }

  ?item wdt:P31 wd:Q5.
  ?item wdt:P570 ?death.
  ?item wdt:P27 wd:Q29999.
  {?item wdt:P106 wd:Q42973.}
  FILTER(YEAR(?death) = 1952)
}
