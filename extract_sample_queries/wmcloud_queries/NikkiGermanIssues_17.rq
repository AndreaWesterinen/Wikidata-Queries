select distinct (?l1 as ?item) (concat(replace(str(?l1),str(wd:),"{{L|"),"}}") as ?l_) {
  ?l1 dct:language wd:Q188;
     p:P9947 ?s1;
     wikibase:lemma ?lemma.
  ?s1 ps:P9947 ?id.
  ?l2 wdt:P9947 ?id.
  filter (?l1 != ?l2).
  filter not exists { ?s1 pq:P9531 ?l2 }
} order by ?lemma
