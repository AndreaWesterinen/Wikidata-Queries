#title: Wrong position for root node
# Complex constraint from [[Property talk:P9764]]
# Position for root node is always 0
SELECT * WHERE {
  ?item p:P5238 ?node.
  ?node ps:P5238 [] ;
        pq:P9763 wd:Q1757074;
        pq:P9764 ?position.
  FILTER(?position != "0")
}
