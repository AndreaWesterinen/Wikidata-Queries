SELECT ?gender ?genderLabel (COUNT(*) AS ?count) WHERE {
  SERVICE wikibase:label { bd:serviceParam wikibase:language "fr". }
  wd:Q36180 (^(wdt:P279*)/^wdt:P106) ?item.
  ?item wdt:P21 ?gender.
}
GROUP BY ?gender ?genderLabel
ORDER BY DESC (?count)
