select distinct (?l as ?item) (concat(replace(str(?l),str(wd:),"{{L|"),"}}") as ?l_) {
  ?l dct:language wd:Q188;
     wikibase:lexicalCategory wd:Q3813849;
     wikibase:lemma ?lemma.
} order by ?lemma
