#Target required claim report for P1906: report listing each item and value

SELECT ?item ?itemLabel ?value ?valueLabel ?snak
WHERE
{
	{
		SELECT DISTINCT ?item ?value ?snak
		WHERE
		{
			{
				?item p:P1906/ps:P1906 ?value .
				BIND("mainsnak" AS ?snak) .
			} UNION {
				?statement0 pq:P1906 ?value .
				?item ?p0 ?statement0 .
				BIND("qualifier" AS ?snak) .
			} UNION {
				?ref pr:P1906 ?value .
				?statement1 prov:wasDerivedFrom ?ref .
				?item ?p1 ?statement1 .
				BIND("reference" AS ?snak) .
			} .
			MINUS { ?value p:P1308 [] } .
		} LIMIT 100
	} .
	SERVICE wikibase:label { bd:serviceParam wikibase:language "en" } .
}
