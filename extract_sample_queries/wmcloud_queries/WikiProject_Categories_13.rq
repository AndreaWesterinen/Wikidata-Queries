SELECT ?cat ?catLabel

WITH { 
  SELECT ?cat WHERE {
      ?cat wdt:P31 wd:Q4167836 
  } OFFSET 3000000 LIMIT 50000
} AS %cats

WHERE {
    INCLUDE %cats
    MINUS {?cat wdt:P971/wdt:P31 wd:Q24571886}
    SERVICE wikibase:label { bd:serviceParam wikibase:language "en" }
  
}
