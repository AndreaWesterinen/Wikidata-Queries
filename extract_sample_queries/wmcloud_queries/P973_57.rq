SELECT ?item ?value WHERE
{
  ?item p:P973/ps:P973 ?value .
  
  FILTER( REGEX(REPLACE(STR(?value), "%20", " "), "^(https?://(www\\.)?allocine\\.fr/personne/fichepersonne_gen_cpersonne=(\\d+)\\.html(.*)?)$") )
}
