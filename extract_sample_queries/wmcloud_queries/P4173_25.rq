SELECT ?item ?value WHERE
{
  ?item p:P4173/ps:P4173 ?value .
  
  FILTER( REGEX(REPLACE(STR(?value), "%20", " "), "^((https://www\\.instagram\\.com/explore/locations/)?([1-9]\\d{5,15})/([a-z0-9\\-]+/?(\\?.+)?)?)$") )
}
