SELECT ?unit (count(?x) as ?count) WHERE {
     { ?x p:P1198/psv:P1198 [ wikibase:quantityUnit ?unit ] }
     UNION
     { ?x pqv:P1198 [ wikibase:quantityUnit ?unit ] }
} GROUP BY ?unit
ORDER BY DESC(?count)
