SELECT (wd:Q5 AS ?item) ?statcount 
WHERE
{
  SELECT (COUNT(DISTINCT ?rel) AS ?statcount) WHERE {
    ?item wdt:P2600 [] ;  wdt:P26 ?rel .
    hint:Prior hint:rangeSafe true .        
    ?rel wdt:P2600 [] .
  }
}
