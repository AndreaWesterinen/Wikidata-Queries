SELECT * WHERE {
  ?lexeme dct:language wd:Q8785;
          wikibase:lexicalCategory wd:Q1084;
          ontolex:lexicalForm ?form.
  ?form ontolex:representation ?representation.
  BIND(LCASE(?representation) AS ?representationL)
  FILTER(?representation != ?representationL)
}
