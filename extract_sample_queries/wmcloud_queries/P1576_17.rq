#title: Claims without source
# Complex constraint from [[Property talk:P1576]]
# Each P1576 claim needs to have at least one (non Wikipedia) source
SELECT DISTINCT ?item { ?item p:P1576 ?claim; wdt:P31 wd:Q5 . OPTIONAL { ?claim prov:wasDerivedFrom ?source . ?source ?p ?v . FILTER NOT EXISTS { ?v wdt:P31 wd:Q10876391 } } FILTER(!bound(?source)) }
