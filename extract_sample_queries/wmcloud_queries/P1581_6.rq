SELECT ?item ?value WHERE
{
  ?item p:P1581/ps:P1581 ?value .
  
  FILTER( REGEX(REPLACE(STR(?value), "%20", " "), "^(https?://(www\\.)?linkedin\\.com/in/([\\-\\&%A-Z0-9a-záâãåäăąćčçéèêěëğîıíłńñøóòôöõřśşșšțúůüýž]+)/?)$") )
}
