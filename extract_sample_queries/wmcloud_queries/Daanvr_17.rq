#title: All coats of arms from an entity located in France.
#defaultView:ImageGrid
# OPTIONAL { ?item wdt:P18 ?img. }
SELECT DISTINCT * WHERE {
  ?item wdt:P94 ?COA;
    wdt:P17 wd:Q142.
  OPTIONAL { ?item wdt:P625 ?geo. }
}
ORDER BY (RAND())
